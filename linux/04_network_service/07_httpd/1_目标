# 安装apache 服务器 (httpd)
# 获取apache帮助信息的途径 (httpd-manual, http://10.1.1.1/manual)
# 通过命令行来查询web服务器的简要描述信息 (httpd -l, -L, -t, etc.)
# apache 的两种工作模式及切换、查询方法
# apache 常用设置指令的作用
# 把网站根目录设定到系统的任意目录
# 设置网站默认主页的文件名
# 打开/关闭网站列目录的功能 (Indexes)
# apache 访问权限allow/deny 的结合原理
# 指令容器Directory, Files, Location 的使用场景，以及三者之间的区别
# 对指定目录的访问进行密码控制
# 和DNS服务器配合，设置基于域名的虚拟主机
# 通过URL映射的方式（别名）让客户端访问不在网站根目录下的文件
# 通过链接的方式让客户端访问不在网站根目录下的文件
# 配置https
